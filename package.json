{
  "name": "cro-audit-ai",
  "version": "0.1.0",
  "private": true,
  "description": "CRO Audit AI - AI-powered Conversion Rate Optimization website analyzer",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web",
    "start": "npm run start --workspace apps/web",
    "lint": "npm run lint --workspace apps/web"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
